const VeinueNav = () => (
  <nav className="fixed top-0 left-0 right-0 z-50 px-6 py-7 md:px-12 flex items-center justify-between"
    style={{ background: 'linear-gradient(to bottom, rgba(8,8,8,0.9) 0%, transparent 100%)' }}>
    <a href="#" className="font-display text-[1.4rem] font-extrabold tracking-[0.08em] uppercase text-foreground no-underline">
      Ve<span className="text-primary">i</span>nue
    </a>
  </nav>
);

export default VeinueNav;
